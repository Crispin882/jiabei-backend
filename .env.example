# ===== 加贝英语台 · 后端配置（复制为 .env 后填写）=====
# 端口（手机通过隧道访问，默认即可）
PORT=3000

# ===== 视觉模型（拍照识别 · 4 字段）=====
# 视觉服务商：qwen=通义千问 / doubao=豆包
VISION_PROVIDER=qwen
# 你的 API Key（通义/豆包控制台创建，免费额度内 ¥0）
API_KEY=
# 通义：默认可不填（用 qwen3-vl-plus，新一代、免费额度内）；豆包：填你的 ARK 模型接入点 ID
VISION_MODEL=qwen3-vl-plus
ARK_ENDPOINT=

# ===== 语音识别（麦克风 → 文本）=====
# 语音服务商：qwen=通义 Paraformer（默认 paraformer-v2，免费额度内 ¥0；旧 paraformer-v1 将于 2026-10-10 下线）/ none=关闭云端ASR(走浏览器)
ASR_PROVIDER=qwen

# 说明：
# - 不填 API_KEY 也能启动：OCR 会用本地 Tesseract 兜底（英文+中文，音标/词性可能需手改）；ASR 会提示走浏览器方案。
# - 填了 API_KEY 且 VISION_PROVIDER/ASR_PROVIDER 正确，即获得“接近完美”的拍照识别与语音识别，免费额度内不花钱。
# - 强烈建议到控制台开启“额度耗尽即停”，防止误扣费。
